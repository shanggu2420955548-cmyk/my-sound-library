# Transcriptionist v3 - Core dependencies
# Install: pip install -r requirements.txt
# 打包环境需额外安装: pyinstaller, pillow（build.bat 会安装）；AI 功能需 onnxruntime、librosa 等（build.spec 会 collect_all）

# GUI（必须）
PySide6>=6.6.0
# PyPI package name; imports are qfluentwidgets and qframelesswindow.
PySide6-Fluent-Widgets>=1.5.0

# Database
SQLAlchemy>=2.0.0
alembic>=1.12.0

# Audio
mutagen>=1.47.0
soundfile>=0.12.0
pygame>=2.5.0
pyloudnorm>=0.1.1

# AI / ML
numpy>=1.24.0
scikit-learn>=1.3.0
librosa>=0.10.0

# Network / Async
aiohttp>=3.9.0
aiofiles>=23.0.0
requests>=2.31.0

# Utils
watchdog>=2.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
openpyxl>=3.1.0
psutil>=5.9.0

# 打包/可选（打包时 build.spec 会 collect_all，开发可按需安装）:
# onnxruntime>=1.16.0 或 onnxruntime-directml（GPU）
# numba（librosa JIT）
# tokenizers（CLAP 文本编码）
# scipy（librosa 依赖）
